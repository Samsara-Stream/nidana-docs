- In 5.1, the example uses `topology()` to define a topology, but the description uses `declare()`. I think I saw in other places too that `topology` is used. Do we need consistency? But in 14.4, I see the examples are all uses `declare()`. 
- About 5.3, I want to see an actual code showing an example of effectful stream sources with switchMap. No need to update the document, I just want to understand better.
- In 7.5, the references aren't updated: "The data contract choice becomes load-bearing if persistence (§16.5) or server-driven topologies (§16.4) are added later.". Also make the references hyperlinks. I think this kind of references (using §) are vestiges from older versions. Please scan and check all of those references.
- In 8.1, I can think of some other cross-cutting concerns common in mobile apps: location (as in lat, lon), OS-level permissions. Is it good to mention those as well or unnecessary?
- Somewhere (probably in motivation), I'd like to mention the reason why I think this architecture makes sense for frontend. Frontend app is fundamentally an asynchronous environment: signal coming from sensors, user interacting, data coming from the backend, all are happening asynchronously. Modeling it as synchronous processes are unnatural (I don't know ya, this is more of my feeling, not sure how to express this).
